<form method="post" action="{{ path('product_tag_delete_tag', {'id': productTag.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete-tag' ~ productTag.id) }}">
    <button class="btn btn-primary" ><i class="fa fa-trash" aria-hidden="true"></i></button>
</form>